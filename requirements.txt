# SO-101 Multi-Dataset Training Pipeline
# Python 3.10+
#
# System dependencies (install BEFORE running pip):
#   - FFmpeg 4-7  (apt install ffmpeg)     — required by torchcodec for video decoding
#   - git-lfs     (apt install git-lfs)    — required for pushing models to HF Hub
#   - CUDA 11.8+  (nvidia drivers)         — recommended for GPU training
#
# Install:
#   ./install.sh              # auto-installs everything
#   # or manually:
#   uv pip install -r requirements.txt

# PyTorch (CUDA wheels auto-selected by pip/uv)
torch>=2.0.0

# Hugging Face Hub
huggingface_hub>=0.26.0
hf_xet

# LeRobot with multi-dataset + Pi0.5 support
# Uses our fork with patches for MultiLeRobotDataset (per-dataset delta_timestamps,
# CLI list parsing, _from_datasets classmethod).
# Installed from source tarball to avoid git-lfs issues with the lerobot repo.
# Once merged upstream, switch back to: lerobot[pi] @ git+https://github.com/huggingface/lerobot.git
lerobot[pi,feetech] @ https://github.com/meetsitaram/lerobot/archive/refs/heads/feature/multi-dataset-support.tar.gz

# Data handling
pandas>=2.0.0
pyarrow>=14.0.0

# Environment / config
python-dotenv>=1.0.0

# Progress output
tqdm>=4.66.0
